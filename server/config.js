const config = {

    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "db4free.net",
      user: "sunandplay",
      password: "sunandplay",
      database: "sunandplay",
    },
    listPerPage: 10,
    
};

module.exports = config;